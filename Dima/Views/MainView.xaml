<UserControl x:Class="Dima.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Right">
            <TextBlock>Ви ввійшли як...</TextBlock>
            <Button Content="Нове замовлення" Command="{Binding AddRequestCommand}"></Button>
            <Button Content="+ інженера"></Button>
            <Button Content="+ бригадира"></Button>
            <Button Content="Вихід" Command="{Binding ExitCommand}"></Button>
        </StackPanel>
        
        <TextBlock DockPanel.Dock="Top" TextAlignment="Center" FontSize="20">
            Замовлення
        </TextBlock>

        <DataGrid IsReadOnly="True"
                  FontSize="16">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Назва"/>
                <DataGridTextColumn Header="Замовник"/>
                <DataGridTextColumn Header="Інженер"/>
                <DataGridTextColumn Header="Стан"/>
            </DataGrid.Columns>
        </DataGrid>
        
    </DockPanel>
</UserControl>
